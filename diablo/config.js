const config = require('dotenv').config();

/*
  Config Bootstrap

  This is the bootstrapping code for applications config.
  You can check diablo/model to see how config is used.
*/

module.exports = config;